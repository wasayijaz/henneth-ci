# desk.henneth.app — the members-only terminal.
#
# Crawling is deliberately ALLOWED here even though every page carries <meta robots="noindex">.
# That is not a contradiction: a crawler has to be able to fetch the page to read the noindex.
# Disallowing instead would leave the URLs eligible to appear in results as bare, description-less
# links that we can no longer suppress — the classic robots.txt/noindex trap.
#
# Marketing SEO lives on https://henneth.app (its own robots.txt and sitemap).

User-agent: *
Allow: /
