requests>=2.31
numpy>=1.26
# Sidereal ephemeris for the astro pillar (scripts/astro_engine.py). Chosen deliberately:
# pure Python, so it needs no C toolchain (pyswisseph will not build on 3.14) and no ephemeris
# data download (skyfield wants a 17MB JPL kernel per environment). Installing it here means the
# cloud cron picks it up from `pip install -r requirements.txt` with no workflow edit — which
# matters, since editing .github/workflows/* needs an OAuth scope this environment doesn't have.
pymeeus>=0.5.12
